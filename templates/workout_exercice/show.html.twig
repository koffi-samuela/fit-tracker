{% extends 'base.html.twig' %}

{% block title %}WorkoutExercice{% endblock %}

{% block body %}
    <h1>WorkoutExercice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ workout_exercice.id }}</td>
            </tr>
            <tr>
                <th>Number_of_repetitions</th>
                <td>{{ workout_exercice.numberOfRepetitions }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ workout_exercice.duration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_workout_exercice_index') }}">back to list</a>

    <a href="{{ path('app_workout_exercice_edit', {'id': workout_exercice.id}) }}">edit</a>

    {{ include('workout_exercice/_delete_form.html.twig') }}
{% endblock %}
