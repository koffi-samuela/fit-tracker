{% extends 'base.html.twig' %}

{% block title %}WorkoutExercice index{% endblock %}

{% block body %}
    <h1>WorkoutExercice index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Number_of_repetitions</th>
                <th>Duration</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for workout_exercice in workout_exercices %}
            <tr>
                <td>{{ workout_exercice.id }}</td>
                <td>{{ workout_exercice.numberOfRepetitions }}</td>
                <td>{{ workout_exercice.duration }}</td>
                <td>
                    <a href="{{ path('app_workout_exercice_show', {'id': workout_exercice.id}) }}">show</a>
                    <a href="{{ path('app_workout_exercice_edit', {'id': workout_exercice.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_workout_exercice_new') }}">Create new</a>
{% endblock %}
